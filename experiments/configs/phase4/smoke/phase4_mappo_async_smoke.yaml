phase: 4
metadata:
  phase: phase4
  purpose: smoke
  status: active
  expected_runtime: short
  gate_relevance:
  - async_vector_env
  lineage: Async vector-env smoke variant for Phase 4.
env:
  seed_base: 3519994490
  opponent_bot: noop
  learner_team: A
  sim:
    round_length_seconds: 3
    fog_of_war_enabled: false
    randomize_map: false
    seed: 3519994490
    action_repeat: 3
    mechanics:
      revolver_damage_centi_hp: 7500
      revolver_fire_cooldown_ticks: 15
      revolver_hitbox_radius: 0.75
      respawn_ticks: 240
model:
  embed_dim: 16
  gru_hidden: 8
  head_hidden: 16
  action_log_std_init: -1.0
ppo:
  num_envs: 2
  rollout_len: 8
  num_epochs: 1
  learning_rate: 0.0003
  value_normalization: true
  vector_env: async
  torch_num_threads: 1
  lr_schedule: constant
  lr_final_ratio: 1.0
  warmup_updates: 0
  clip_ratio: 0.2
  value_clip_ratio: 0.2
  gamma: 0.997
  gae_lambda: 0.95
  entropy_coef: 0.01
  value_coef: 0.5
  max_grad_norm: 0.5
run:
  total_updates: 1
  eval_every: 1
  eval_episodes: 1
  checkpoint_every: 1
  log_every: 1
  output_dir: runs/phase4_mappo_async_smoke

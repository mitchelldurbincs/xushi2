"""Training loops for xushi2.

The training entrypoint is `train.train:main` (see `train.py`). The curriculum
is laddered per docs/rl_design.md §6 — each phase is a separate config file
under experiments/configs/.
"""
